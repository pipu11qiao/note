const obj = {
    A: 1,
    j: 11,
    Q: 12,
    K: 13
};

function judge(arr) {
    //max min
    const max = Math.max.apply(null, arr);
    const min = Math.min.apply(null, arr.filter(item => item > 0))
    if (max - min >= 5) {
        return false
    }
    return true
}

const arr = [0, 0, 4, 2, 5];
console.log(judge(arr));

fun
try{
    await(next())
}
check
/age
static



fun2
